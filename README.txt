To run the application:

1. Install the required libraries in the environment.
2. Run the caption_generator.ipynb file that generates the model for generating image caption.
3. Run the python file with images as an input using command "generate_caption.py" -i "img.jpg"
4. The image file opens along with the caption and twitter opens.